const colors = {
    redPrimary: '#940a0a',
    backgroundWhite: 'rgb(250, 246, 246)',
};

const effects = {
    boxShadow: '3px 3px 10px rgba(0, 0, 0, 0.25)'
};

const theme = {
    ...colors,
    ...effects
};

export default theme;
